[www]
pm = dynamic

; 8c/16g single-node (Laravel + MySQL)
pm.max_children = 32
pm.start_servers = 8
pm.min_spare_servers = 8
pm.max_spare_servers = 16

; recycle workers to cap memory growth over time
pm.max_requests = 500
