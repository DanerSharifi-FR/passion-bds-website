[www]
pm = dynamic

; =========================
; PREVIOUS: 8c/16g single-node (Laravel + MySQL)
; =========================
; pm.max_children = 32
; pm.start_servers = 8
; pm.min_spare_servers = 8
; pm.max_spare_servers = 16

; =========================
; NEW: 4c/8g single-node (Laravel + MySQL)
; =========================
pm.max_children = 16
pm.start_servers = 4
pm.min_spare_servers = 4
pm.max_spare_servers = 8

; recycle workers to cap memory growth over time
pm.max_requests = 500
